#include <iostream>
#include <vector>
#include <queue>
#include <stack>
#include <set>
#include <unordered_set>
#include <unordered_map>
#include <map>
#include <algorithm>
#include <cmath>
#include <string>
#include <sstream>
#include <bitset>
#include <utility>

using namespace std;

void fast() {
    ios::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
}

class Solution {
public:
    int kConcatenationMaxSum(vector<int>& arr, int k) {
        // 1 <= arr.length <= 105
        // 1 <= k <= 105
        // -10^4 <= arr[i] <= 10^4

        // Unless the entire arr is the max subarray, no point multiplying?

    }
};

int main() {
    fast();

    return 0;
}

